# Repository-wide ignores only. Rules that belong to a single module or
# directory live in a .gitignore inside it.

## ide
.idea/

## misc
bin/rust-analyzer
.log

## flakes
result

# git-hooks.nix (auto-generated)
.pre-commit-config.yaml

# Claude Code
.claude/settings.local.json
.claude/worktrees/
