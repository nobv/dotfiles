# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking

# See: https://nikitabobko.github.io/AeroSpace/guide#config-version
config-version = 2

# 'break' and 'shelf' are the workspaces AeroSpace itself knows about — the
# domain-independent ones (see desk-model note below). Keep them alive even when
# empty and invisible, matching what config-version = 1 inferred automatically
# from this file's keybindings.
persistent-workspaces = ['break', 'shelf']

# Desk model: each desk (domain) owns one AeroSpace workspace, switched exclusively
# via the desk CLI — never bind desk switching to an AeroSpace key here. A window's
# default home is its own desk's tiled surface (no on-window-detected rule needed).
# Three exceptions, all domain-independent (design D6 addendum):
#   - floating windows are the "wagon": they follow you across desk switches instead
#     of staying tiled on one desk.
#   - 'break' is the shared workspace (bound to alt-b) for life/comms apps that don't
#     belong to a single desk; its residents stay tiled, not floating, so they don't
#     get swept along as wagon passengers.
#   - 'shelf' is the always-on reference surface, pinned to a non-main monitor by the
#     force-assignment below; like break, its residents are tiled (floating would make
#     them wagon passengers). Desks and break time-share the main monitor; the shelf
#     sits off to the side. A desk gets no force-assignment (D8: no domain names in
#     this layer) — desk switch moves the active desk onto main at runtime instead.

# You can use it to add commands that run after login to macOS user session.
# 'start-at-login' needs to be 'true' for 'after-login-command' to work
# Available commands: https://nikitabobko.github.io/AeroSpace/commands
after-login-command = []

# You can use it to add commands that run after AeroSpace startup.
# 'after-startup-command' is run after 'after-login-command'
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = []

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 30

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Possible values: (qwerty|dvorak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
key-mapping.preset = 'qwerty'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# Fallback value (if you omit the key): on-focused-monitor-changed = []
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']
on-focus-changed = ['exec-and-forget ~/.config/aerospace/follow-aqua-voice.sh']

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See: https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
inner.horizontal = 10
inner.vertical = 10
outer.left = 10
outer.bottom = 10
outer.top = 10
outer.right = 10

# Pin the domain-independent workspaces to monitors. The shelf fallback array covers
# every rig from one line: home (1 external + built-in) and clamshell (2 external)
# resolve 'secondary'; the lid-open 3-way (2 external + built-in) has no 'secondary'
# (that pattern is 2-monitor-only) and falls through to 'built-in'. The spare face on
# a 3-way rig is left unassigned — the desk self-heals onto main on the next switch.
[workspace-to-monitor-force-assignment]
break = 'main'
shelf = ['secondary', 'built-in']

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
# Fallback value (if you omit the key): mode.main.binding = {}
[mode.main.binding]

# All possible keys:
# - Letters.        a, b, c, ..., z
# - Numbers.        0, 1, 2, ..., 9
# - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
# - F-keys.         f1, f2, ..., f20
# - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon, backtick,
#                   leftSquareBracket, rightSquareBracket, space, enter, esc, backspace, tab
# - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
#                   keypadMinus, keypadMultiply, keypadPlus
# - Arrows.         left, down, up, right

# All possible modifiers: cmd, alt, ctrl, shift

# All possible commands: https://nikitabobko.github.io/AeroSpace/commands

# See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
# You can uncomment the following lines to open up terminal with alt + enter shortcut (like in i3)
# alt-enter = '''exec-and-forget osascript -e '
# tell application "Terminal"
#     do script
#     activate
# end tell'
# '''

# See: https://nikitabobko.github.io/AeroSpace/commands#layout
alt-slash = 'layout tiles horizontal vertical'
alt-comma = 'layout accordion horizontal vertical'

# See: https://nikitabobko.github.io/AeroSpace/commands#focus
alt-h = 'focus left'
alt-j = 'focus down'
alt-k = 'focus up'
alt-l = 'focus right'

# See: https://nikitabobko.github.io/AeroSpace/commands#move
alt-shift-h = 'move left'
alt-shift-j = 'move down'
alt-shift-k = 'move up'
alt-shift-l = 'move right'

# See: https://nikitabobko.github.io/AeroSpace/commands#resize
alt-shift-minus = 'resize smart -50'
alt-shift-equal = 'resize smart +50'

# Desk switching itself happens via the desk CLI, not AeroSpace workspace keys.
# 'break' is the one shared, domain-independent workspace — see the desk-model
# note near the top of this file. See: https://nikitabobko.github.io/AeroSpace/commands#workspace
alt-b = 'workspace break'
# Send the focused window to 'break' (the on-window-detected rules below only
# place windows at launch; this is the manual escape hatch for the rest).
# See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
alt-shift-b = 'move-node-to-workspace break'

alt-shift-f = 'fullscreen'

# See: https://nikitabobko.github.io/AeroSpace/commands#workspace-back-and-forth
alt-tab = 'workspace-back-and-forth'
# See: https://nikitabobko.github.io/AeroSpace/commands#move-workspace-to-monitor
alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'

# See: https://nikitabobko.github.io/AeroSpace/commands#mode
alt-shift-semicolon = 'mode service'

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
esc = ['reload-config', 'mode main']
r = ['flatten-workspace-tree', 'mode main'] # reset layout
f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
backspace = ['close-all-windows-but-current', 'mode main']

# sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
#s = ['layout sticky tiling', 'mode main']

alt-shift-h = ['join-with left', 'mode main']
alt-shift-j = ['join-with down', 'mode main']
alt-shift-k = ['join-with up', 'mode main']
alt-shift-l = ['join-with right', 'mode main']

# on-window-detected places a window at launch only (alt-b / alt-shift-b are the
# manual escape hatches for the rest). A window with no rule stays on its own desk,
# tiled — that is the default and the common case. The rules below are only for the
# three domain-independent categories (design D6). Sort a window by asking, in order:
#
#   1. Wagon  — is it a TOOL I keep using no matter which desk I'm at?
#               → floating. floating is the carry marker: desk switch drags it along.
#   2. Shelf  — is it REFERENCE material I want in view but don't drive?
#               → tiled on 'shelf', parked on a side monitor, never carried.
#   3. Break  — is it PERSONAL life/comms, off the clock?
#               → tiled on 'break', the shared off-desk room reached by alt-b.
#
# float vs tiled = carried vs settled. Shelf and break are both tiled (settled) and
# split only by reference-vs-personal; a work-only tool that isn't carried needs no
# rule — it settles on its own desk. Only floating things ride the wagon.

# Wagon — a tool used across desks; floating so desk switch carries it along
[[on-window-detected]]
if.app-id = 'com.1password.1password'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.electron.aqua-voice'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.openai.chat'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.anthropic.claudefordesktop'
run = 'layout floating'

# Break — personal life/comms, off the clock; tiled on the shared 'break' workspace
[[on-window-detected]]
if.app-id = 'com.apple.iCal'
run = 'move-node-to-workspace break'

[[on-window-detected]]
if.app-id = 'com.culturedcode.ThingsMac'
run = 'move-node-to-workspace break'

[[on-window-detected]]
if.app-id = 'com.apple.reminders'
run = 'move-node-to-workspace break'

[[on-window-detected]]
if.app-id = 'jp.naver.line.mac'
run = 'move-node-to-workspace break'

[[on-window-detected]]
if.app-id = 'com.apple.Music'
run = 'move-node-to-workspace break'

# Shelf — reference kept in view but not driven; tiled on the off-monitor 'shelf'
[[on-window-detected]]
if.app-id = 'com.apple.finder'
run = 'move-node-to-workspace shelf'

[[on-window-detected]]
if.app-id = 'md.obsidian'
run = 'move-node-to-workspace shelf'

[[on-window-detected]]
if.app-id = 'org.zotero.zotero'
run = 'move-node-to-workspace shelf'

[[on-window-detected]]
if.app-id = 'com.amazon.Lassen'
run = 'move-node-to-workspace shelf'

