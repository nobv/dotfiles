-- Fetched by lazy.nvim only for its editor/nvim.lua, which config/keymaps.lua
-- loads after LazyVim's default keymaps so its <C-h/j/k/l> override wins.
-- The herdr side needs the same repo installed as a herdr plugin
-- (`herdr plugin install paulbkim-dev/vim-herdr-navigation`) plus the
-- keys.command bindings in modules/development/cli-tools/herdr/config.toml.
return {
  { "paulbkim-dev/vim-herdr-navigation", lazy = true },
}
